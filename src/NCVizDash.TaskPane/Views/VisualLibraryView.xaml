<UserControl x:Class="NCVizDash.TaskPane.Views.VisualLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:NCVizDash.TaskPane.ViewModels"
             d:DataContext="{d:DesignInstance vm:VisualLibraryViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource MaterialDesignToolBarBackground}"
                Padding="12,0">
            <TextBlock Text="Visuals"
                       FontWeight="SemiBold" FontSize="13"
                       VerticalAlignment="Center" />
        </Border>

        <!-- Chart type grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding AvailableVisuals}"
                          Margin="6">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:VisualTypeEntry}">
                        <md:Card Width="80" Height="80"
                                 Margin="4"
                                 Padding="6"
                                 Cursor="Hand"
                                 md:ElevationAssist.Elevation="Dp1"
                                 ToolTip="{Binding DisplayName}"
                                 PreviewMouseLeftButtonDown="VisualTile_PreviewMouseLeftButtonDown">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="20" />
                                </Grid.RowDefinitions>

                                <md:PackIcon Grid.Row="0"
                                             Kind="{Binding IconKey}"
                                             Width="32" Height="32"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             Foreground="{DynamicResource PrimaryHueMidBrush}" />

                                <TextBlock Grid.Row="1"
                                           Text="{Binding DisplayName}"
                                           FontSize="9"
                                           TextAlignment="Center"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}" />
                            </Grid>
                        </md:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
